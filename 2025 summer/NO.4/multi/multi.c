#include<stdio.h>

typedef struct ListNode{
    int coefficient;
    int index;
    struct ListNode *next;
} ITEM;

int main()
{
    ITEM *LineOne = NULL,*LineTwo = NULL,*p = NULL;
    while (p->index != 0){
        scanf("%d%d",p->coefficient,p->index);
    }
}