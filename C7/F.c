#include<stdio.h>
#include<math.h>

int getlight(int a, int b);
int getb(int b);
int alight(int b);

int light[14];
int main()
{
    int t,n,k,a,b,sum;
    scanf("%d",&t);
    for (int i = 0 ; i < t ;i++ ){
        scanf("%d%d",&n,&k);
        for (int j = 0; j < k; j++ ){
            scanf("%d%d",&a,&b);

        }
    }
    return 0;
}

int getlight(int a,int b){
    int lightLong = getb(b);
    for (int i = 0; i < lightLong; i++ ){
        
    }
}